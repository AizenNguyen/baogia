#include<iostream>
using namespace std;
short n,ans;
string s;
void back(){
	
}
int main(){
	ios_base::sync_with_stdio(0);cin.tie(0);
	cin>>n;
	back(1);
	cout<<ans;
	for(auto &x:as)
		cout<<x<<'\n';
}